name: metallb
project: default
destination:
  namespace: metallb
source:
  repoURL: https://metallb.github.io/metallb
  chart: metallb
  targetRevision: 0.15.3
  helmValues: |
    crds:
      enabled: true
syncOptions:
  - CreateNamespace=true
