name: cert-manager
project: default
destination:
  namespace: cert-manager
source:
  repoURL: https://charts.jetstack.io
  chart: cert-manager
  targetRevision: v1.17.1
  helmValues: |
    crds:
      enabled: true
syncOptions:
  - CreateNamespace=true
