name: semaphore
project: default
destination:
  namespace: semaphore
source:
  repoURL: https://sergiotocalini.github.io/helm-charts
  chart: sergiotocalini/ansible-semaphore
  targetRevision: v2.10.22
  helmValues: |
    replicaCount: 1
syncOptions:
  - CreateNamespace=true
