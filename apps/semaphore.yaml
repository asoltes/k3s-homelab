name: semaphore
project: default
destination:
  namespace: semaphore
source:
  repoURL: https://semaphoreui.github.io/charts/
  chart: semaphore
  targetRevision: 16.0.11
  helmValues: |
    service:
      type: ClusterIP
    ingress:
      enabled: true
      ingressClassName: traefik
      hosts:
        - host: semaphore.zeromnd.com
          paths:
            - path: /
              pathType: Prefix
    replicaCount: 1
    admin:
      create: true
      username: admin
      password: bsit113092
syncOptions:
  - CreateNamespace=true
