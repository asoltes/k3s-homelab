name: semaphore
project: default
destination:
  namespace: semaphore
source:
  repoURL: https://semaphoreui.github.io/charts/
  chart: semaphoreui/semaphore
  targetRevision: 16.0.11
  helmValues: |
    replicaCount: 1
syncOptions:
  - CreateNamespace=true
