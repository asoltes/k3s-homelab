name: semaphore
project: default
destination:
  namespace: semaphore
source:
  repoURL: https://semaphoreui.github.io/charts/
  chart: semaphore
  targetRevision: 16.0.11
  helmValues: |
    service:
      type: ClusterIP
    ingress:
      enabled: true
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        external-dns.alpha.kubernetes.io/hostname: runbooks.zerok3s.com
      hosts:
        - host: runbooks.zerok3s.com
          paths:
            - path: /
              pathType: Prefix
    replicaCount: 2
    admin:
      create: true
      username: admin
      password: bsit113092
syncOptions:
  - CreateNamespace=true
