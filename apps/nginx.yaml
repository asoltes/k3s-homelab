name: nginx
project: default
destination:
  namespace: nginx
source:
  repoURL: https://charts.bitnami.com/bitnami
  chart: nginx
  targetRevision: 1.29.6
  helmValues: |
    service:
      type: ClusterIP
    replicaCount: 2
syncOptions:
  - CreateNamespace=true
